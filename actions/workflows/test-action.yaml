---
version: '2.0'

stackstorm_demo.test-action:
  tasks:
    delegate:
      # [165, 55]
      action: core.noop
      publish:
        hello: "hello world"
      on-success:
        - run

    run:
      action: core.local
      inputs:
        cmd: >
          echo "{{ _.hello }}"

